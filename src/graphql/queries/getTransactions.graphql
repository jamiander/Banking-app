query getTransactions($accountId: uuid!){
  Transactions(where: {accountId: {_eq: $accountId}}){ 
  	id
    createdAt
    updatedAt
    amount
    description
    category
    status
    transactionDate
    postDate
    accountId
  }
}
